# Rebel Invoice Pro UI

Lightweight Next.js (TypeScript) UI for the Rebel Invoice FastAPI backend.

## Setup
```bash
cd frontend
npm install
npm run dev
```

## Environment
Create `frontend/.env.local` if needed:
```
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_API_KEY=dev-key
```
